export type CollectionNames = "chimpers" | "chimpersGenesis" | "ssow";
// | "FishBallerz";

export type ReactionMetadata = {
  title: string;
  scale: number;
  x: number;
  y: number;
  filename: string;
};
